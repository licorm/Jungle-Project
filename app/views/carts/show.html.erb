<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <%= render "cart_items" if enhanced_cart.length > 0 %>

  <%= render "empty_cart" if enhanced_cart.length === 0 %>
  
  
</section>

